import React from 'react';
import {StyleSheet, Text} from 'react-native';


// Custom TimelineItem component
export const DoseForm = () => {

  return (
      <Text style={styles.main}>Debug</Text>
  );
};

const styles = StyleSheet.create({
  main: {
    color: 'red',
    backgroundColor: 'blue',
    fontSize: 30
  }
});
